name: struct_daily
config_version: 1
class_name: SimpleCheckpoint
validations:
  - batch_request:
        datasource_name: filesystem
        data_connector_name: runtime_data_connector
        data_asset_name: struct_daily
        runtime_parameters:
            path: data/derived/struct_daily.parquet
        batch_spec_passthrough:
            reader_options:
                engine: pyarrow
    expectation_suite_name: struct_daily_suite
