# Runbook — Derivado Dinámico

## Pre-requisitos
- Infra local corriendo: Postgres, MinIO, Prefect, MLflow.
- Dataset `data/raw/draws.csv` o `data/raw/draws.parquet`.

## Pasos (smoke primero)
1) Validar `draws` (smoke):
